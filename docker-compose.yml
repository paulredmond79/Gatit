services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"   # host:container — nginx listens on port 80 (see Dockerfile EXPOSE 80)
    restart: unless-stopped
